{
  "name": "",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "rm -rf ./dist && npm run build && ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "typescript": "latest"
  }
}
