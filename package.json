{
  "name": "",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "start": "tsc --build --clean && tsc && ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
